# VS Code OS: initramfs configuration for an installed system.
# The live medium's archiso hooks are removed by the installer and replaced
# with this plain, disk-booting hook set.
HOOKS=(base udev autodetect microcode modconf kms keyboard keymap consolefont block filesystems fsck)
COMPRESSION="zstd"
