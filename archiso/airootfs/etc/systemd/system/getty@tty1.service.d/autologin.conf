# VS Code OS: log the kiosk user in on tty1 so the graphical session can start
# without a display manager.
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin vscodeos --noclear --noissue %I $TERM
Type=idle
