# Compressed swap in RAM. Electron is memory-hungry and a Pi has little to
# spare, so this keeps VS Code responsive instead of hitting the SD card.
[zram0]
zram-size = min(ram / 2, 4096)
compression-algorithm = zstd
swap-priority = 100
